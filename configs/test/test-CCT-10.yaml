test_dataset:
  dataset:
    name: image-folder
    args:
      #root_path_1: /home/SCISR/SCI1K-Test/X2/Bicubic
      root_path: /home/CCT

  wrapper:
    name: sr-implicit-downsampled-cascaded
    args:
      batch_size: 1
      scale_max: 10
      scale_base: 10
      phase: valid

  batch_size: 1

eval_type: benchmark-10
eval_bsize: 30000

data_norm:
  inp: {sub: [0.5], div: [0.5]}
  gt: {sub: [0.5], div: [0.5]}