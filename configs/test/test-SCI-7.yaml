test_dataset:
  dataset:
    name: image-folder
    args:
      #root_path_1: /home/SCISR/SCI1K-Test/X2/Bicubic
      root_path: /home/SCISR/SCI1K-Test/X7/HR

  wrapper:
    name: sr-implicit-downsampled-cascaded
    args:
      batch_size: 1
      scale_max: 7
      scale_base: 7
      phase: valid

  batch_size: 1

eval_type: benchmark-7
eval_bsize: 30000

data_norm:
  inp: {sub: [0.5], div: [0.5]}
  gt: {sub: [0.5], div: [0.5]}